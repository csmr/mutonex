# Data Directory

This directory (`infra/data`) is the project's data root, defined by `DATA_HOME` in `scripts/app.config.sh`.

## PostgreSQL

-   `./postgres`: Data cluster.
-   `./conf`: Configuration files (`postgresql.conf`, `pg_hba.conf`).
